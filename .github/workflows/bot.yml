name: meclis-belge-bot

on:
  schedule:
    # TATIL TEMPOSU - 1 Ekim oncesi geri alinacak (saatlik + MAX 12)
    # Gunde 3 gonderi. Hedef TRT 09:17/13:17/21:17.
    # GitHub zamanlayicisi 40-90 dk geciktigi icin 45 dk one alindi.
    # UTC yazilir; TRT = UTC+3
    - cron: "32 5 * * *"
    - cron: "32 9 * * *"
    - cron: "32 17 * * *"
  workflow_dispatch:         # elle tetikleme

permissions:
  contents: write

concurrency:
  group: bot
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Çalıştır
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          MAX_POSTS_PER_RUN: "1"
          POST_GAP_SECONDS: "150"
        run: node src/run.js

      - name: state.json commit
        if: always()
        run: |
          git config user.name "meclis-belge-bot"
          git config user.email "bot@users.noreply.github.com"
          git add state.json
          git diff --cached --quiet || git commit -m "state: $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
